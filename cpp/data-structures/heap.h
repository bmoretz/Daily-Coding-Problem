#pragma once

#pragma once

#include <algorithm>
#include <memory>
#include <ostream>
#include <vector>

namespace data_structures::heap
{
	template<typename Ty>
	class heap
	{
		std::unique_ptr<Ty[]> data_{};

		std::size_t size_{}, capacity_{};
		
	public:

		ct
	};
}